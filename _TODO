grep -r TODO *

current:
	- delete permission functionality 
		- tell distro needs to tell local daemon

	- switch unsigned char * stuff to strings

	- config for esod, and esol (global config?)
	- java api
		- encrypt
			- asym (evp interface for rsa?)
		- decrypt
			- asym
		- sign
		- verify

	- change exits to exceptions
		- current: uds connect

All:
	- authenticated communication

	- using '!' as message terminator means we cannot send 'hello world!'...

Central Auth:

    - web interface
        - create new set
			- query to see if cred/perm already exists
			- create user/pass set
        - view current set
			- delete perm functionality
			- add/edit perm: error handling, string cleaning
			- login to edit permissions
		- flask templates
	- daemon
		- config file for distro locations
		- propagate all
			- later: propagate changes
		- time of last propagation?
		- config file with distribution servers
    - database
		- clear query strings
		- logging queries also log the keys -_-
		- handle username/password input
		- seed prng for generating rsa keys
		- encrypt keys before storing to disk
		- remove unneeded functions from impl?
        - backup mechanism for database
		- ensure integrity of database

Distribution Servers:
    - distribution daemon
		- config file for port

Host Daemon:
    - cronjob
    - save esod pid
    - close all file descriptors? (23)
    - implement protocol
    - key management
    - database / encryption (298)
        - clean strings
        - handles errors
        - triggers
        - hash database entries to ensure integrity?
        - prepared statements
    - authenticate client
    - signal handling (712)
    - secure directory (45, 55)
	- generalize output path
	- integrity checks (encrypt/hash)
    - delete esod tests
    - sanitize env? (6)
    - error 'constant' files

Client API:
    - jni interface
	- easier way to load the shared library

Crypto:
	- aes.h
		- use IV
	- hmac.h
		- add more hash types
