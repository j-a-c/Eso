grep -r TODO *

Central Auth:
	- fix building creds and perms
		- remove print statements

	- replace atoi with strtol
    - web interface
        - create new set
			- query to see if set already exists
            - fill other params in credentials
				- algo, key, size,...
        - view current set
            - c++ impl
			- pull from database
			- edit / add / remove functionality
	- daemon
		- tcp socket for propagation / backup
    - database
		- remove unneeded functions from impl?
        - backup mechanism for database

Distribution Servers:
    - distribution server

Host Daemon:
    - cronjob
    - save esod pid
    - close all file descriptors? (23)
    - implement protocol
    - key management
    - database / encryption (298)
        - clean strings
        - handles errors
        - triggers
        - hash database entries to ensure integrity?
        - prepared statements
    - authenticate client
    - safely erase data (706)
    - signal handling (712)
    - secure directory (45, 55)
    - logging
	- generalize output path
	- add logging levels
	- integrity checks (encrypt/hash)
    - delete esod tests
    - sanitize env? (6)
    - error 'constant' files

Client API:
    - jni interface
